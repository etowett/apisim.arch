apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: live

resources:
  - namespace.yaml
  - ../../base

patchesStrategicMerge:
  - rollout-replica.yaml
  - hpa.yaml

images:
  - name: apisim
    newName: gcr.io/citizix-one/apisim
    newTag: latest
