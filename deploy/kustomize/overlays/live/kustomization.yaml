apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: live

resources:
- namespace.yaml
- ../../base

patchesStrategicMerge:
- rollout-replica.yaml
# - hpa.yaml
images:
- name: gcr.io/citizix-one/apisim
  newTag: main-0f8650b
